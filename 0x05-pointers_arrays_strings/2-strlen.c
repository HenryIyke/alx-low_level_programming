#include "holberton.h"
#include <stdio.h>
/**
 * _strlen - Code to print the length of a string
 * @c: Int value for the argument of the function
 * Return: Always 0
 */

int _strlen(char *s)
{
return(_strlen)
}
