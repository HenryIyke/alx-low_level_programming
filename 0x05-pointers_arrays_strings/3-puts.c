#include "holberton.h"
#include <stdio.h>
/**
 * _puts - Code to print string to standard output
 * @str: string value for the argument of the function
 * Return: void
 */

void _puts(char *str)
{
char c[100] = *str;
for (c = 0; c <= _strlen; c++)
{
_puts(c);
}
}
